{
  "name": "mood-journal",
  "private": true,
  "version": "1.0.0",
  "description": "Mood journal with Firebase Auth + Firestore (npm modular SDK + Vite)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "firebase": "^11.6.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
